// StartMenu — reserved for future extraction of the home screen from App.jsx
// Currently the home screen lives entirely inside App.jsx (screen === 'home' block).

export default function StartMenu() {
  return null;
}
