// Results — reserved for future extraction of the win/loss result screen from App.jsx
// Currently the result screen is rendered inline inside App.jsx (gameStatus === 'finished' block).

export default function Results() {
  return null;
}
